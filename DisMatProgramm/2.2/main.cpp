#include <iostream>
#include <vector>

using namespace std;

void inputVector(vector<int> &v) {
  for (auto &i: v)
    cin >> i;
}

void permutation(vector<int> v, vector<> int i) {
  vector<int> p(v.size());
}

int main() {
  int n;
  cin >> n;
  vector<int> v(n);
  inputVector(v);



  return 0;
}
